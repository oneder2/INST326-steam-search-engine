# Core FastAPI Backend Dependencies - Ultra Minimal for Deployment
# Only the absolute essentials needed to run the FastAPI backend

# Core FastAPI Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# HTTP and Data Processing
httpx==0.25.2
requests==2.31.0

# Security and Validation
python-multipart==0.0.6

# Environment Configuration
python-dotenv==1.0.0

# Performance and Utilities
python-dateutil==2.8.2

# Note: sqlite3 is built into Python and doesn't need to be installed
# Note: This ultra-minimal set should be sufficient for basic FastAPI deployment
# Note: ML libraries (faiss, sentence-transformers) can be added later when needed
